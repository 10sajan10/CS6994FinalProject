version: '3.8'

services:
  postgres:
    build: .
    container_name: postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: database
    ports:
      - "5433:5432"
    volumes:
      - lro_pgdata:/var/lib/postgresql/data

volumes:
  lro_pgdata:
